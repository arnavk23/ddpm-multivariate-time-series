def evaluate_model(model, test_data):
    """
    Evaluates the performance of the trained model on the test dataset.

    Parameters:
    model: The trained model to be evaluated.
    test_data: The dataset on which the model will be evaluated.

    Returns:
    metrics: A dictionary containing evaluation metrics such as loss and accuracy.
    """
    # Placeholder for evaluation logic
    metrics = {
        'loss': None,
        'accuracy': None
    }

    # Implement evaluation logic here
    # For example, you might want to run the model on the test_data
    # and calculate the loss and accuracy.

    return metrics